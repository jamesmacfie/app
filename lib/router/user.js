'use strict';Router.route('/login', {		name: 'login'});Router.route('/logout', {		name: 'logout',		onBeforeAction: function() {				Meteor.logout();				this.redirect('loggedOut');				this.next();		}});Router.route('/loggedOut', {		name: 'loggedOut',		onBeforeAction: function() {				if (Meteor.userId()) {						this.redirect('home');				}				this.next();		}});