'use strict';Router.route('/login', {	layoutTemplate: null,	name: 'login',	data: function() {		var params = this.params.query;		return {			emailAddress: params.emailAddress		};	}});Router.route('/logout', {		name: 'logout',		onBeforeAction: function() {				Meteor.logout();				FlashMessages.sendSuccess('You have been logged out.');				this.redirect('login');		}});Router.route('/signUp', {	layoutTemplate: null,	name: 'signUp',	waitOn: function() {		return Meteor.subscribe('avatarImages');	}});