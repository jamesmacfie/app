'use strict';Router.route('rooms', {		name: 'roomList',		data: function() {				return {						rooms: Rooms.find(),						title: 'Your rooms'				};		},		waitOn: function() {				return Meteor.subscribe('userRooms');		}});Router.route('rooms/insert', {		name: 'roomInsert'});Router.route('rooms/:_id', {		name: 'roomItem',		data: function() {				return Rooms.findOne(this.params._id);		},		waitOn: function() {				return Meteor.subscribe('room', this.params._id);		}});Router.route('rooms/:_id/addSensor', {	name: 'roomAddSensor',	data: function() {		return {			room: Rooms.findOne(this.params._id),			sensors: Sensors.find(),			title: 'Add a new sensor'		};	},	waitOn: function() {		return [			Meteor.subscribe('room', this.params._id),			Meteor.subscribe('userSensors')		];	}});